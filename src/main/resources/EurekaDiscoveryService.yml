spring:
  profiles:
    active: "local"
  application:
    name: EurekaDiscovery

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false

server:
  port: 8083